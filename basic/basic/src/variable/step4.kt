package variable

/**
 * 동등성 : 객체의 상태가 같은지
 * 동일성 : 객체의 주소값이 같은지
 *
 * 자바에서의 동등성 : equals 사용
 * 자바에서의 동일성 : == 사용
 *
 * 코틀린에서의 동등성 : == 사
 * 코틀린에서의 동일성 : === 사용
 *
 * in / !in 연산자
 * - 컬렉션이나 범위에 포함되어 있다, 포함되어있지 않다
 *
 * a..b
 * - a ~ b꺼지의 범위 객체를 생성
 *
 * 연산자 오버로딩 : 객체마다 연산자를 직접 정의할 수 있다.
 *
 * */